# git init
Which create a new repo

# git add <filename>
git config --global user.name "Malik"
git config --global user.email "gfqw@gmail.com"
git add . or git add *
git commit -m "Commit message"
git push origin main
git pull url
